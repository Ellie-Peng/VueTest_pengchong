language: node_js
node_js:
 - "10.15.3"
dist: trusty
sudo: false

branches:
 only:
 - master

cache:
 directories:
   - node_modules

install:
 - npm install -g yarn

script:
 - npm run-script test
  - npm run-script build
deploy:
 provider: pages
 skip_cleanup: true
 github_token: $519e89932706ffb87f0e824b4b5111d12e303be8
 local_dir: dist
 on:
   branch: master
